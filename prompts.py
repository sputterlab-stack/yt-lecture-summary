# 集中管理 LLM prompt 常數
from pathlib import Path

import yaml

_TAXONOMY_PATH = Path(__file__).parent / "category_taxonomy.yaml"


def _load_taxonomy() -> dict:
    if not _TAXONOMY_PATH.exists():
        return {"taxonomy": {}, "aliases": {}, "skip_files": []}
    return yaml.safe_load(_TAXONOMY_PATH.read_text(encoding="utf-8")) or {}


def _build_taxonomy_text(taxonomy: dict) -> str:
    """二層樹序列化成 prompt 可讀文字。"""
    lines = []
    for main, subs in taxonomy.items():
        if subs:
            sub_str = " / ".join(subs)
            lines.append(f"- **{main}**：{sub_str}")
        else:
            lines.append(f"- **{main}**（子類自由命名）")
    return "\n".join(lines)


_TAXONOMY = _load_taxonomy()
TAXONOMY_TEXT = _build_taxonomy_text(_TAXONOMY.get("taxonomy", {}))


SYSTEM_PROMPT = """你是學術級演講內容拆解專家。你的任務是用第一性原理（First Principles）拆解演講逐字稿，產出「上下兩層」結構：上層精選摘要（沒時間就看這段），下層 8 段完整拆解（深入學習用）。"""

# 注意：USER_PROMPT_TEMPLATE 用 .format(yt_title=..., source_url=..., ...) 被呼叫，
# {TAXONOMY_TEXT} 必須在這層用 string replace 預先填入（不走 .format()，避免被當成 placeholder）
_RAW_USER_PROMPT = """【分類提示】
從以下二層 taxonomy 選一組「主類 + 子類」：

{TAXONOMY_TEXT}

規則：
- **主類必須**從上面選一個現有的，**不可新建**
- 子類優先選列出的現有子類；確實都不適合可新建簡短中文（4-8 字）
- 若主類後標「子類自由命名」，則子類自訂

tags 是 3-6 個自由標籤，描述細分主題、關鍵概念、講者特色，方便未來搜尋。

【拆解原則】
1. 不是流水帳重述，是還原演講者的「思考鏈」
2. 找出底層假設（演講者預設了什麼前提才能推出結論）
3. 區分「事實聲明」「推論」「個人立場」「實踐建議」
4. 反例與對立觀點必須保留（演講者如何回應 X 反對意見）
5. 任何外語原文，摘要全部用繁體中文表達；金句段落保留原文 + 中譯
6. 上層精選摘要約 300-500 字
7. 下層 8 段完整拆解約 1500-2500 字

【元資料】
- YT 原標題：{yt_title}
- 來源 URL：{source_url}
- 影片時長：{duration}
- 偵測語言：{language}

【逐字稿】
{transcript}

【輸出格式】嚴格 JSON（無 markdown code fence），格式如下：
{{
  "filename": "10-20字精準繁體中文標題（不含副檔名與標點 \\\\ / : * ? \\" < > |）",
  "speaker": "若可從內容識別則填講者姓名，否則填 '未知'",
  "language": "原語言（中文/英文/日文等）",
  "category": "從 taxonomy 選的主類（必須是上面列出的）",
  "subcategory": "從 taxonomy 該主類下的子類（或合理新命名 4-8 字）",
  "tags": ["細分標籤", "跨主題標籤"],
  "thesis": "演講者最核心的主張（30 字內，立場句不是描述句；要能單獨拿出來引用，例如『美聯儲必須立刻降息以避免長債失控』）",
  "weekly_action": "這週能立刻嘗試的一個具體小動作（不超過 50 字，要能在 5 分鐘內開始做；不是抽象建議，是可執行的單一動作，例如『今天記錄一次自己刷手機的衝動以及當時的情緒』）",
  "summary_md": "完整 Markdown 字串，依下列結構生成"
}}

【summary_md 結構規範】

summary_md **不可**包含 H1 標題（不要寫「# 標題」，外層程式會自動加）。
summary_md 從以下內容開始，依序生成：

第一行：30 字內的 elevator pitch，使用 > blockquote 格式

## 精選摘要（沒時間先看這段）

### 核心重點
3-5 條編號清單，每條格式為：
1. **{{重點標題}}**：{{1-2 行說明}}

### 核心概念
3-6 個 bullet points，每個格式為：
- **{{概念名}}**：{{一行內定義 10-30 字}}

---

## 完整拆解（深入學習）

### 一、核心問題（演講要回答的根本問題）
### 二、底層假設（推論前提）
### 三、核心概念定義
### 四、論證鏈（從假設推到結論的路徑）
### 五、關鍵證據與案例
### 六、反例與對立觀點的處理
### 七、可操作的洞察（讀者聽完能做什麼）
### 八、金句精華（原文 + 中譯，3-5 句）

8 段每段一個 H3 標題，標題文字完全一致，順序不可調換。"""

# 把 TAXONOMY_TEXT 預先填入；其餘 {yt_title} {transcript} 等仍由 summarizer.py 的 .format() 處理
USER_PROMPT_TEMPLATE = _RAW_USER_PROMPT.replace("{TAXONOMY_TEXT}", TAXONOMY_TEXT)


# === 導讀 prompt（線性帶入；enrich_intro.py 用）===

INTRO_SYSTEM = """你是學術導讀作者。將演講摘要轉換成「線性導讀」— 帶讀者跟著演講者的思考軌跡走一遍。先有 narrative scaffolding，讀者再去看心智圖網路才能掛得上去。"""

INTRO_USER_TEMPLATE = """根據以下演講完整拆解，寫一段「導讀（線性帶入）」給讀者。

【規格】
- **依演講內容深度自然展開**（通常 800-1500 字，不為達字數注水）
- **第三人稱敘事**（「演講者指出...」「他從 X 出發...」「接著推到 Y...」）
- **禁止**：列表 / 子標題 / 子彈點 / 表格 / markdown code fence — 要的是**線性流動**的故事感，不是分點
- 段落用空行分隔（純文字段落，不用 ##）

【字數紀律】
- **文章精神必須帶到** — 太簡略 = 看了等於沒看，這是失敗
- **禁止贅字**、避免**重複同一個論點**（每個觀點只出現一次，從不同角度推進敘事而非反覆強調）
- 內容深度決定長度，不為字數犧牲精神也不為字數注水

【內容結構建議】（不必硬切標籤，自然敘事即可）
1. 為什麼這個問題重要 / 演講者要回答什麼根本問題
2. 演講者從什麼前提出發（底層假設）
3. 主要論證走哪條路（從假設到結論的推論軌跡，含關鍵證據與反例處理）
4. 結論的意義 / 對讀者的啟發

【寫作要求】
- 不要重述演講內容，要還原演講者的**思考路徑**
- 用連接詞推進故事感（「然而」「進一步地」「在這個基礎上」「最終」）
- 抓 1-2 個關鍵概念當錨，圍繞它們展開
- **絕不寫**「這篇演講...」「演講者表示...」這種 meta 描述開頭，直接從內容切入

【演講標題】{title}

【完整拆解】
{full_breakdown}

【輸出】只輸出導讀內文（純散文，不含「## 導讀」標題、不含 markdown code fence、不含 meta 描述）。第一字直接開始正文。"""


# === 乾貨摘要（30 秒 catch-up）prompt（時間戳 beats；讀 .srt 逐字稿；enrich_digest.py + web_server /digest 用）===

DIGEST_SYSTEM = """你是「30 秒 catch up」摘要師。讀者沒看影片，要在 30 秒內跟著你把整支影片的脈絡走一遍、並知道想深入時該跳到哪個時間點。你產出的是「跟著影片時間軸的故事線」，不是抽離的重點卡。"""

DIGEST_USER_TEMPLATE = """根據以下「帶時間戳的逐字稿」，做一張讓人 30 秒 catch up 整支影片的摘要。

【輸出純文字，三段固定結構，順序不可換，每段標題獨立成行、段間空一行】

💡 一句定位
一句話講清楚：這支影片在談什麼 ＋ 最核心的那個主張／轉變（看完這句就抓到重點，30-50 字）。

⏱ 跟著影片走
依影片實際順序列 4-7 個關鍵 beat，每個 `• ` 開頭，格式為：
• [mm:ss] 標題：一句話說明
規則：
- 時間戳 [mm:ss] 必須取自逐字稿、對應該重點「實際開始」的位置，由小到大遞增
- 標題盡量寫成「從 X → Y」的轉變或對比（例：增長→融資、印鈔機→建造商、好消息→壞消息），最好記；真的沒有對比再用名詞標題
- 說明保留具體數字、比喻、人名（例：600→1100、地心引力、Tom Lee）
- 每個 beat 一行、40 字內，能斷就斷短；抓真正的轉折，不要流水帳

📌 so-what
一句收束：看完整支的結論／對讀者的意義（30-50 字）。

【格式紀律】
- 全繁體中文（外語專有名詞、人名可留原文）
- 不要 markdown 粗體 `**`、不要 `#` 標題、不要 code fence、不要表格
- 三段各用「💡 一句定位」「⏱ 跟著影片走」「📌 so-what」當標題獨立成行
- bullet 一律用半形「• 」開頭，不用 markdown 的 `-`、`*`、數字編號

【影片標題】{title}

【帶時間戳逐字稿】
{transcript}

【輸出】只輸出摘要內文，第一行直接是「💡 一句定位」，不要任何前言、開場白或結語。"""


# === 邏輯拆解 prompt（第一性原理推導鏈 × 多視角壓力測試；enrich_logic.py + web_server /logic 用）===

LOGIC_SYSTEM = """你是第一性原理拆解專家。你不重述演講內容，而是把一套主張剝到「不能再質疑的地基」，再一步步推回結論，並從多個視角壓力測試關鍵環節。目標是讓讀者能自己把這條邏輯重推一遍、並看出它哪裡硬、哪裡可能崩。"""

LOGIC_USER_TEMPLATE = """把以下演講拆解成「第一性原理 × 多視角」的邏輯骨架。不要重述、不要流水帳，要還原並檢驗推理本身。

【輸出純文字，四段固定結構，順序不可換，每段用「一、」「二、」這種標題獨立成行】

一、拆到地基（不可再質疑的底層事實）
列 2-4 條這套主張最終立足的「地基」——物理、數學、經濟、制度或人性上幾乎不可違反的事實／約束。每條 `• ` 開頭、一句話，並用括號標明它為何是地基（例：(物理) (製造現實) (賽局結構) (人性)）。地基是「事實」不是「觀點」，挑真的拆不下去的。

二、從地基推上來（推導鏈）
從地基一步步推到講者的核心結論。每一步換行、用「→」開頭遞進，4-7 步為宜。每步開頭標性質：
[事實] / [推論] / [假設] / [價值判斷]
讓讀者一眼看出整條鏈哪裡硬（事實／推論）、哪裡軟（假設／價值判斷）。

三、多視角壓力測試
挑這條鏈最關鍵的 2-3 個轉折點，逐一測試。每個轉折先寫「◆ 測試：<哪一步>」，下面三行各一個視角：
🟢 最強支持：從某角度，這步最站得住的理由
🔴 最大破口：從對立角度，這步最可能錯／被推翻的地方
⚖️ 第一性裁決：回到地基，這步到底成不成立、成立到幾分
視角可取自：看多／看空、短期／長期、供給／需求、個體／系統、誘因／結構、不同學科或利益方——挑真正製造張力的對立角度，不要硬湊。

四、崩潰條件
列 1-3 條「載重假設」：哪一個前提一旦為假，整條結論就垮。每條 `• ` 開頭、一句話、可被觀察／證偽。這是讀者之後該持續盯的監測點。

【紀律】
- 全繁體中文（外語專有名詞可留原文）
- 不要 markdown 粗體 `**`、不要 `#` 子標題、不要 code fence、不要表格
- bullet 用半形「• 」；推導鏈用「→」與換行；區與區之間空一行
- 寧可少而硬，不要多而虛：每一句都要有資訊量，禁止贅述與重複同一論點

【演講標題】{title}

【完整拆解】
{full_breakdown}

【輸出】只輸出邏輯骨架內文，第一行直接是「一、拆到地基（不可再質疑的底層事實）」，不要任何前言、開場白或結語。"""


# === Active Recall 挑戰 prompt（web_server /challenge endpoint 用）===

RECALL_CHALLENGE_SYSTEM = """你是嚴格但有同理心的學習教練。使用者剛看完一篇演講摘要，現在以「合上摘要、用自己的話講」的方式接受 active recall 挑戰。你的工作是把使用者的回答跟原摘要對齊，找出他抓到什麼、漏什麼。只輸出 JSON，不輸出任何其他文字。"""

RECALL_CHALLENGE_USER_TEMPLATE = """對照原摘要評估這位使用者的「合上書」回答。

【任務】
1. **got_right**：使用者抓對的核心點（list[str]，每項 1 句、20 字內）。只列「真的抓到」的點，不要太寬鬆。
2. **missed**：使用者**漏掉但很重要**的核心點（list[str]，每項 1-2 句、40 字內）。只列「會影響理解」的關鍵漏失，雞毛蒜皮不用列。每項用「忽略了 X」開頭。
3. **coaching**：一句話引導下次該注意的方向（30 字內，鼓勵性而非批評，例如「下次注意分辨主張與證據的差異」）。

【特殊情況】
- 使用者根本沒寫東西或寫亂碼 → got_right=[], missed 列 3 個最關鍵點, coaching="建議先閱讀後再嘗試挑戰"
- 使用者寫得很完整 → missed 可能 1 個甚至 0 個（不要硬挑毛病）
- 使用者偏離主題 → got_right=[], missed 列原摘要核心點, coaching 提醒回到主題

【演講原摘要（精選段）】
{summary_top}

【使用者的回答】
{user_answer}

【輸出格式】嚴格 JSON：
{{"got_right": ["...", "..."], "missed": ["...", "..."], "coaching": "..."}}"""


# === Mermaid 心智圖 prompt（gen_mindmap.py 用）===

MINDMAP_SYSTEM_PROMPT = """你是 mermaid mindmap 心智圖專家。將演講摘要 .md 轉成 mermaid mindmap 語法，供本地 VS Code / Obsidian / GitHub 渲染。"""

MINDMAP_USER_PROMPT_TEMPLATE = """將以下演講摘要轉換為 mermaid mindmap 心智圖。

【規格】
- 用 mermaid `mindmap` 語法（不是 graph TD）
- 根節點：演講標題（精簡至 < 15 字），用 `((標題))` 形狀，可用 `<br/>` 換行
- 第一層：5-7 個主要章節（依該篇實際結構，常見有：核心問題 / 底層假設 / 核心概念 / 論證 / 證據 / 反例 / 洞察 / 金句）
- 第二層以下：每章節 2-4 個子節點
- 節點文字 < 25 字（太長會擠）
- 總節點數 30-50（少了空，多了亂）
- 善用 mindmap 形狀：`((中心))` / `(分支)` / `[葉節點]`
- 縮排：兩格一層

【輸出格式】
**只輸出 mermaid mindmap 語法本身**，不要有任何前後文字、不要 markdown code fence（不要寫 ```mermaid）、不要解釋。第一行必須是 `mindmap`。

【演講摘要】
{markdown}
"""


# === 融會貫通 prompt（整理後的一段式 elevator pitch；enrich_synthesis.py + web_server /synthesis 用）===

SYNTHESIS_SYSTEM = """你是把一場演講「融會貫通」成一段話的高手。讀者讀完你寫的這一段，就能掌握整場的核心主張、它為什麼成立、以及該帶走什麼——像一段精煉到位的 elevator pitch，而不是把重點逐條再講一遍。"""

SYNTHESIS_USER_TEMPLATE = """把以下演講「融會貫通」成一段連貫的短文，讓讀者只讀這一段就掌握整場精華。

【目標】
不是逐點重述、不是流水帳，而是把散落的重點串成一條清楚的脈絡：講者的核心主張是什麼、建立在什麼前提上、怎麼推到結論、對讀者意味著什麼。讀完像聽完一段精煉的 elevator pitch。

【格式紀律】
- 只輸出「一段」連貫白話散文（150–250 字），不分點、不條列、不用 markdown 粗體/標題/code fence/表格
- 全繁體中文（外語專有名詞、人名可保留原文）
- 開門見山：第一句就點出整篇在談什麼＋最核心的主張；中段把關鍵邏輯與證據串起來；收尾一句講清楚 so-what（對讀者的意義）
- 保留關鍵的具體數字、比喻、人名，讓它有資訊密度，不要空話套話

【影片標題】{title}

【完整拆解】
{full_breakdown}

【輸出】只輸出那一段短文，不要任何前言、標題或結語。"""
