# 迭代日誌（DEVLOG）

> 每次改動記：**為什麼要改 → 量到什麼 → 決定什麼（含被推翻的） → 改了什麼 → 怎麼驗的**。
> 只記「下次的人需要知道」的東西；規格看 `SYSTEM_SPEC.md`，用法看 `README.md`。

---

## 2026-08-23 — 下載被 YouTube 擋掉（403）+ 補上持久化紀錄檔

### 起因

使用者回報：「貼上連結之後會有錯誤發生，看 log 是無法下載音訊，好像是被阻擋」。
接著發現**根本沒有 log 可看**——本專案從頭到尾只有 `print`，訊息只活在那個黑視窗裡。

### 量到什麼（成對對照，不是猜的）

同一台機器、同一個網路、同一支影片（`dQw4w9WgXcQ`）、前後數分鐘內：

| 下載器版本 | 結果 |
|---|---|
| `2026.03.17`（專案 venv 內，跑兩次） | `HTTP Error 403: Forbidden`，兩次都失敗 |
| `2026.08.19`（最新，裝在隔離目錄） | 成功，下載 3.27 MiB |

唯一變因是版本。正例（新版成功）與反例（舊版失敗）都實跑到。
最後一次成功產出是 `outputs/transcripts/*.srt` 的 08-17，故障發生在 08-17～08-23 之間。

**結構性發現**：`transcriber.download_audio()` 的 `ydl_opts` 設了 `no_warnings: True`。
yt-dlp 本來會自己喊「你的版本超過 90 天沒更新」，**這個設定把唯一會提前示警的聲音摀住了**，
所以使用者只看得到「被阻擋」，看不到原因。

### 決定（第二意見：已過 — Codex 稽核，收據 `20260823-192147-378428_review.log`）

Codex 推翻我三處，我照它的結論做：

1. **我原本要在 `.gitignore` 加 `outputs/logs/` → 純假動作。** `.gitignore` 早就整個忽略 `outputs/`，
   再加一行對 git 行為零影響。**取消。**
2. **我原本的驗收 V2 寫「POST /api/submit」→ 這個 endpoint 不存在**（實際是 `/convert`）。
   而 V3「跑完 `download_audio()` 後斷言 log 含 task_id」**按設計不可能通過**——
   `download_audio()` 根本沒有 task_id。驗收改成全部走 `/convert` 這個真實入口。
3. **我原本的錯誤提示寫「9 成是版本問題」→ 沒有根據的機率宣稱。** 改成中性但可執行：
   直接給出「用本程式的 Python 更新」的完整指令（**不能寫裸 `pip`，會裝到別的環境**），
   並列出其他可能（需登入／地區限制／網路被擋）。

它也否決了「背景自動更新 yt-dlp」（不該在使用者不知情時改動環境）與「三份文件同步寫一樣的段落」。
並指出一個措辭問題：**B1/B3 不是預防機制，是偵測機制**——它們不會讓故障不發生，
只把「發現＋修復」從好幾天縮短成一條指令。

### 改了什麼

- **升級**：專案 venv 的 yt-dlp `2026.03.17` → `2026.08.19`；
  `requirements.txt` 改成 `yt-dlp>=2026.8.19`（下限，不釘上限——YouTube 是移動目標）。
- **`transcriber.py`**：
  * 移除 `no_warnings: True`（`quiet` 保留，它只壓進度條）。
  * 新增 `_warn_if_stale_ytdlp()`：版本超過 90 天就寫一筆警告，每個 process 只寫一次。
    **只在 `download_audio()` 內呼叫**，不放模組頂層（不能破壞 commit 1ab3c09 的冷啟動成果）。
  * 新增 `_download_hint()`：只有被擋型的錯誤（403／Forbidden／需登入）才附上下一步，
    其他錯誤保留原文不亂猜。
- **新增 `applog.py`**（41 行）：`outputs/logs/app.log`，2 MB 輪替留 5 份、UTF-8 明講
  （Windows 預設 cp950 會炸中文）、同時保留 console 輸出、初始化 idempotent。
- **`web_server.py`**：`traceback.print_exc()` → `log.exception()`（前者只進 console，
  視窗一關就查不到，這正是這次查不出原因的理由）；task 開始／每次步驟切換／個別階段完成各記一筆。
  **logging 的 file IO 放在 `_TASKS_LOCK` 之外**，不拉長鎖的持有時間。
- **`README.md`**：輸出檔案表加紀錄檔一列；新增「十一、貼了連結卻失敗？」。
  依 Codex 意見**不動 `SYSTEM_SPEC.md`**（logging 尚未是對外契約）、不動 setup 腳本。

### 驗了什麼

| 項目 | 結果 |
|---|---|
| **V1 真實下載（走專案自己的 `download_audio()`，不是 CLI）** | ✅ 回傳 5,114,348 位元組的 mp3，標題與片長（213 秒）都正確 |
| 偵測器靈敏度（正例＋反例各自實跑） | ✅ 8/8：403／需登入會補提示、一般錯誤不補；230 天前的版本會警告、3 天前的不警告、同一次只警告一次 |
| **驗證腳本本身的反例** | ✅ 把預期改壞一格 → exit code 變 1（證明它真的會擋，不是只會印字） |
| 全部 `.py` 編譯 + 7 支入口 import | ✅ |
| V5 冷啟動（啟動到 port 5000 答得出來） | 0.537 秒（單次取樣；1ab3c09 當時量到 0.38 秒，仍在 1 秒內） |
| **V2/V3 端到端（貼連結 → 出摘要）** | ✅ 補跑通過（見下一節）。首次嘗試時這台機器對外網路整個中斷（連 `8.8.8.8:443` 都不通），等 15 分鐘恢復後才跑得起來 |

### 兩個要記下來的坑

1. **背景指令的結束代碼被我自己蓋掉。** 我把 `... > out.txt 2>&1` 後面接了
   `echo "EXIT=$?" >> out.txt`，於是外層看到的是 `echo` 的成功——**背景通知回報 exit 0，
   實際上驗收腳本是 exit 1**。要判斷成敗的指令後面不要再接任何東西。
2. **`app.log` 有一筆我無法歸屬的紀錄**（19:34:04，我的測試影片下載時連線逾時）。
   已排除：專案內沒有殘留暫存檔、沒有本專案的 python 程序還活著、我的探針產物自 19:29 起沒再變動。
   內容與當時開始的網路中斷一致，且只牽涉我的測試影片、沒碰到任何既有摘要。**記在這裡是因為查不出來就該寫下來，不是因為它無害。**

---

## 2026-08-23（續二）— 把「下載器過舊」搬到畫面上，並給一個能按的更新鈕

### 起因：我以為做完的「提醒」，在使用者的用法下等於不存在

修完 403 之後我加了兩個提醒：版本逾 90 天寫 WARNING 進 `app.log`、
403 類錯誤訊息附上 pip 指令。使用者看完的回應是：

> 「如果再有同樣的情況 **請程式提醒**要更新python（**假如程式本身可以按須更新更好**）」

也就是說兩個都不算數：**紀錄檔他不會開**（他是雙擊 `開啟UI.bat` 的使用者，不開終端機），
**錯誤訊息只在已經壞掉之後才出現**，而且那行指令要他自己開終端機貼。
⇒ **log 不是使用者介面。** 提醒要在他唯一會看的地方（dashboard），而且要能當場處理。

### 決定（第二意見：已過 — Codex 稽核，收據 `20260823-210241-857478_review.log`）

它改了我的設計五處，我全部照做：

1. **不准用錯誤訊息的文字當開關。** 我原本打算前端比對 error 字串裡有沒有「403」。
   它指出那是脆弱耦合——文案一改、翻譯一次、yt-dlp 換句話說，橫幅就靜默失效。
   改成 `transcriber` 丟出帶欄位的自訂例外 `DownloadBlocked`，task 記
   `failure_kind` / `suggested_action`，前端只看 `suggested_action`。
2. **90 天不是我猜的門檻，要講清楚依據**：yt-dlp 官方保證至少每 90 天一個 stable release，
   逾期代表落後一個發布週期。**但不能只靠時間**，被擋時要立刻叫（不看天數）；
   也**不能改成只在失敗後才提醒**，那就失去唯一的事前預警。
3. **我的併發保護有時序漏洞（TOCTOU）**：檢查「沒有 task 在跑」與開始跑 pip 之間，
   使用者仍可能按下轉換。改成 `/convert` 與更新共用同一把 `_UPDATE_LOCK`，
   檢查與建立 task 在同一個臨界區內，並把 `_PENDING` 與批次執行中一起納入判斷。
4. **狀態查詢要處理三種壞情況**（套件不存在／版本看不懂／查詢失敗），
   **不准默默當成「沒過舊」**——查不出來一律出聲。
5. **fail closed**：`sys.executable` 不在專案 venv 底下就拒絕更新（400），
   免得把系統 Python 更新掉。

它也點名我方案裡的假動作：**只寫 README／DEVLOG 不算提醒**（使用者出事時不會翻文件）；
**只有 endpoint 沒有可見入口**等於沒做；**更新成功只跳一次 alert**，按掉就沒了。

### 改了什麼

- **`transcriber.py`**：新增 `DownloadBlocked`（帶 `failure_kind` / `suggested_action`）、
  `ytdlp_status()`（**用 `importlib.metadata` 查版本，不 import yt_dlp 本體**，
  冷啟動 0.38 秒不能為了顯示一行字吐回去）、`STALE_DAYS = 90` 並附上依據。
- **`web_server.py`**：`GET /ytdlp/status`、`POST /ytdlp/update`（只在使用者按下時跑，
  逾時 300 秒，用新 process 查更新後版本——本 process 的套件中繼資料可能是舊快取）、
  `/convert` 與更新雙向互斥、task 新增兩個結構化欄位、首頁把版本狀態傳進樣板。
  `needs_restart` 的判準是**更新當下 `"yt_dlp" in sys.modules`**：沒載入過就直接生效，
  載入過才要求重開——**不假裝按完就好了**。
- **`templates/index.html`**：轉換框上方的橫幅（黃＝過舊、紅＝被擋、綠＝已更新）＋
  「立即更新」按鈕；需要重開時**停用轉換鈕**，免得使用者拿記憶體裡的舊模組白跑一次。

### 驗了什麼

**API 層 22 項全過**（Flask test client，不佔用使用者正在用的 5000 埠）。
關鍵的是三個「★」項——它們證明的不是「有擋」，是**擋的時候真的沒有去跑 pip**：

| 項目 | 結果 |
|---|---|
| 有轉檔在跑 → 409，**且 pip 呼叫次數 = 0** | ✅ |
| 更新中 → `/convert` 回 409，**且 task 數量沒有增加** | ✅ |
| 不是專案 venv 啟動 → 400，**且 pip 呼叫次數 = 0** | ✅ |
| 正例：沒人在跑 → 真的呼叫 `pip install -U yt-dlp`、回傳新版本 | ✅ |
| 版本狀態四種情況（過舊／正常／套件不存在／版本看不懂）各自的答案 | ✅ |
| 橫幅：過舊時展開且寫出天數、正常時 `display:none`、套件不存在時講人話 | ✅ |

**真實 HTTP 層另外驗一輪**（port 5001，真的起 server、真的按更新、真的跑 pip）：
過舊時頁面上真的看得到橫幅與按鈕、版本正常時橫幅收起、按下去真的更新成功。
渲染後的 JS 過 `node --check`。

### 刻意不做

- **自動更新／背景更新／啟動時偷偷更新**：那是在使用者不知情時改他的環境。
- **更新後自動重啟伺服器**：會殺掉正在跑的轉檔，而且雙擊啟動的視窗被關掉後
  使用者不知道發生什麼事。**寧可老實說「請重開」，也不要程式自己動手。**
- 更新其他套件（torch / whisper 風險完全不同，與本次故障無關）。

---

## 2026-08-23（續）— 收尾失敗把成功的摘要改判失敗 ＋ 純數字標籤炸掉索引

### 起因：修好下載之後，端到端驗收仍然只差一項

真實影片走 `/convert` 全程跑完、`.md` 與 `.srt` 都寫好了，**task 最終狀態卻是 `error`**。
往下追：`_run_batch_postprocess()` 的 `gen_index.py` 崩潰 →
`_finalise_waiting_tasks(ok=False)` 把所有 `waiting_batch` 的 task 一起標成 error。

**使用者看到的是紅色失敗卡片，但摘要其實已經好好地躺在硬碟上。**

崩潰點 `gen_index.py:101` `", ".join(e["tags"])` →
`TypeError: sequence item 5: expected str instance, int found`。
全庫掃描 163 篇：**恰好 1 篇**的 tags 是 `[..., 2026]`，最後一個是 int。
根因在**寫入端**：`summarizer.py` 產 frontmatter 時 `tags: [{', '.join(tags)}]` 沒有加引號，
`2026` 進檔後是裸 scalar，YAML 讀回來就變 int。**寫的時候是字串，讀回來變數字。**

**這個 bug 已經潛伏一段時間**：`INDEX.md` mtime 停在 08-16 15:02，但 08-17 有成功的轉檔產物
——那次的批次也失敗了，只是跟「下載失敗」混在一起，看起來就是一團錯誤。

### 決定（第二意見：已過 — Codex 稽核，收據 `20260823-201700-938208_review.log`）

**Codex 把我的順位整個掉頭**：我送審的是「修標籤」，它判定 **P0 是錯誤傳播，不是標籤**。

> 「摘要成功寫出後，不應被共用批次工作的失敗改判成轉檔失敗。」

它還指出我沒看到的第三個缺陷：`_finalise_waiting_tasks()` **沒有綁定本輪的 task id**
——批次執行期間新提交的 task 若進入 `waiting_batch`，會被上一輪的批次提前結案。

它同時否決我兩處：
1. 我打算抽一支共用的 `normalize_frontmatter()` → **現在不要抽**。目前只有兩個消費端，
   各自在 YAML 邊界轉 `list[str]` 更清楚；真出現第三個再抽**窄的** `normalize_tags()`。
2. 我寫的弱點 W3「沒有修資料就修不好」→ **不成立**。有了讀取端正規化，
   既有的 int tag 已經不會讓批次崩潰；修資料的價值是恢復資料不變式，不是解除故障。
3. 我原本的 V2 只打算證明 `str(t)` 能轉型 → **那是假測試**，必須實際走 `gen_index` 的路徑。

### 改了什麼

- **`web_server.py`（P0 錯誤隔離）**：
  * `_run_batch_postprocess()`：四支腳本**互相獨立，一支失敗不跳過其餘**，最後彙總錯誤；
    失敗寫進紀錄檔。舊版第一支掛掉就整批中止（索引壞了順便讓心智圖也不更新）。
  * `_finalise_waiting_tasks(task_ids, ...)`：**只結算本輪**（新增 `_ROUND_TASKS` / `_ROUND_LOCK`），
    而且**一律標 done**——批次失敗改記在 `_BATCH_STATE["last_error"]`，不倒灌成 task 失敗。
  * `/tasks` 回傳新增 `batch.last_error`。
- **`templates/index.html`**：批次狀態列在失敗時顯示
  「⚠ 摘要已存好，但索引／心智圖更新沒跑完」，不再讓使用者以為轉檔失敗。
- **`gen_index.py`**：讀 tags 時 `[str(t) for t in ...]`，與 `web_server.py:213` 早就有的寫法一致。
- **`summarizer.py`**：寫 frontmatter 時每個 tag 都過 `_yaml_quote()`（thesis / weekly_action 早就這樣做）。
- **資料修復**：`2026外星人訊息與中共清算預言拆解.md` 的 tags 行加引號。
  只改那一行，sha256 `251f6878…` → `ff4f0f53…`。**這是動使用者的資料，記在這裡。**

### 驗了什麼

**批次隔離（注入真的會失敗的腳本，不是把結果假造回來）**：把 `BASE_DIR` 指到暫存目錄，
放四支假腳本、其中一支必定 exit 1，走真的 subprocess、真的錯誤彙總、真的結算函式。**9 項全過**：

| 項目 | 結果 |
|---|---|
| ★ 摘要已寫好的 task 仍然是 `done`，且沒有被塞錯誤訊息 | ✅ |
| 失敗那支之後的腳本仍然有跑（四支全部留下執行痕跡） | ✅ |
| 反例：本來就 error 的 task 不會被洗成成功 | ✅ |
| 反例：不屬於這輪的 task 不會被提前結案（仍是 `waiting_batch`） | ✅ |
| `/tasks` 真的吐得出 `batch.last_error`（前端才顯示得了） | ✅ |

**標籤修復**：`gen_index.py` 改前實跑 exit 1（TypeError），改後 exit 0、
`INDEX.md` 從停留在 08-16 變成重新產出 **164 篇**；全庫非字串 tag 由 1 降為 **0**。

**端到端（真實入口，11 項全過）**：啟動 server → 壞連結 → 真實 19 秒影片：

```
V5 冷啟動 0.54 秒 ·V4-1~4 壞連結標 error 且紀錄檔含 task id / ERROR / traceback
V3-1 真實連結跑到 done ← 這一項就是這次修的
V3-2/3 產出 .md 與 .srt · V3-4/5 紀錄檔含每一步與「個別階段完成」· 清理回收成功
```

前端：渲染後的 JS 過 `node --check`，新的提示字串確認出現在頁面上。

### 三個要記下來的坑（都是我自己的驗收工具在說謊）

1. **背景指令的結束代碼被我自己蓋掉**：`... > out.txt 2>&1` 後面接了 `echo "EXIT=$?"`，
   外層看到的是 `echo` 的成功——**通知回報 exit 0，實際腳本 exit 1**。
2. **驗收腳本的判定式寫法錯**（把字串當函式呼叫）→ 整支腳本在最後一哩崩潰，
   而它崩潰前**已經花掉一次完整的 LLM 轉檔**。
3. **用位元組長度去切字串**：`log_before = 檔案大小`，但 `str[log_before:]` 是按字元切，
   中文一個字 3 個位元組 → 切掉的比預期多很多，於是「紀錄檔含 task id」誤判成失敗。
   **紀錄檔一長就會出現，短的時候測不出來。**

⇒ 三次都是「**我為了檢查別人而寫的東西自己壞掉**」。所以驗收腳本現在一律先跑
**自我校驗（故意錯的預期必須被判 FAIL）**，過不了就直接 assert 掛掉。

---

## 2026-08-16（傍晚）— 標籤列收納 ＋「隱藏」改成可取回的「收摺」

### 起因（使用者看完 UI 後的回饋）

> 1. 「首頁搜尋欄底下有一大堆關鍵字 icon，我覺得有點喧賓奪主，應該要收納起來」
> 2. 「更多操作裡面的隱藏，按了之後似乎沒有 undo 的地方，就會直接消失，這個設計不好。
>    我是覺得隱藏可以變成類似摺疊這樣，可以去摺疊頁面取回，而不是消失」

### 量到什麼

**標籤**：163 篇共 **829 個不重複標籤**，其中 **91.2% 只出現在一篇**；
出現 ≥2 篇的只有 73 個、≥3 篇的 27 個、≥5 篇的 6 個。
→ 那 756 個單篇標籤當 chip **只能篩出一張卡**，和直接打字搜尋完全重疊，
卻佔滿整個首頁。這不是排版問題，是那些 chip 本來就沒有篩選價值。

**隱藏**：取回的入口其實**早就存在**（`btn-show-hidden`），但它
①塞在搜尋輸入框裡面 ②`n === 0` 時 `display:none`。
所以使用者按下隱藏之前根本沒見過它，按下之後也不會注意到它冒出來。
**這是可發現性壞掉，不是功能缺失**——所以修法是換位置與換模型，不是加功能。

### 改了什麼

- `web_server.py` `index()`：`all_tags` 從 `list[str]` 改成 `[{tag, count}]`，
  依篇數由多到少排序。
- `templates/index.html`：
  - 標籤列**整條預設收起**，改由「🏷 標籤篩選」按鈕展開；收起時若正在用標籤篩選，
    旁邊顯示「篩選中：X、Y」（否則會以為畫面壞了）。
  - 展開後**只顯示 ≥2 篇的 73 個**（chip 上帶篇數），單篇標籤加 `.tag-rare` 收起，
    另有「顯示全部（含 756 個單篇標籤）」開關。
  - **「隱藏」改名為「收摺」**，語意從「消失」變成「移到別的地方」。
  - 新增**已收摺區**（頁面最下方）：`📁 已收摺 N 篇`，點開列出每一篇 + 「取回」鈕，
    另有「全部取回」。沒有收摺任何東西時整區不顯示。
  - 移除搜尋框裡的 `btn-show-hidden`、`body.show-hidden` 機制、`card-hidden-badge`
    與其 CSS（都是舊模型的殘留）。
  - `rebuildTagChips()` 改吃 `Map<tag, count>`，與 Jinja 那條路徑排出相同結果。
  - localStorage key **沿用 `yt_hidden_v1`**，所以先前已隱藏的篇章會自動出現在收摺區，不會遺失。

### 驗了什麼（SSR 與轉檔後重繪兩條路徑都跑）

標籤：`chipsTotal=829 rare=756 barCollapsed=true shownWhenOpen=73 shownWhenAll=829`
收摺：`panelHiddenAtStart=true cardGoneFromGrid=true panelVisible=true foldedCount=1
listRows=1 rowIsTheCard=true afterRestore_cardBack=true afterRestore_panelHidden=true
stored=2 countAfter2=2 afterRestoreAll_stored=0 afterRestoreAll_panelHidden=true`
重繪後：`chips=829 rare=756 barStillCollapsed=true clearBtns=2 foldAfterRerender=1 restoredOK=true`

### ⚠️ 驗收時的一個 sha256 差異（已查明，非本次改動）

`outputs/summaries/王虹邓煜菲尔兹奖与挂谷猜想解读.md` 在 **15:17:45** 多了一段
`## 融會貫通`（內容完整正常）。**是使用者在 `/catchup` 頁點「▶ 生成融會貫通」產生的**
（`templates/catchup.html:399` 的按鈕 → `genSynthesis()` → `POST /synthesis/<f>/generate`
→ 寫回 `.md` → `location.reload()`）。本次程式改動全部只讀不寫
（`split_body` / `extract_essence` 是純函式，`collect_summaries` 只 `read_text`）。已重新取基準。

🔴 **我當下的判斷是錯的，錯法值得記下來**：我先前寫「瀏覽器上沒有任何路徑會寫這一段」，
根據是 `grep -n "synthesis\|generate" templates/catchup.html | head -12` ——
**`head -12` 把輸出截斷在 CSS 區塊，真正的 `fetch(... /generate)` 在第 550 / 564 行，
從沒進到我眼前**，我卻拿這個被截斷的結果下了「不存在」的結論。
**截斷過的搜尋結果不能拿來證明「不存在」**；要證明不存在，至少得看完整輸出或用計數。

---

## 2026-08-16（下午）— 入口收斂版：把早就寫好卻沒接上的「精粹」接到卡片上

### 起因（使用者當天稍晚的裁示，推翻同日早上的排序）

> 「**學習效果不在抽考環節。對我來說，易於理解的演說精粹才是重點，這樣是消化內化吸收。
> 我現在在乎的是這個環節，使用到最少的注意力然後有最大的效益。**」

早上做的抽考探針被使用者判定不是重點。**這是排序錯誤，不是實作錯誤**——
記錄一下錯在哪：我把「主動提取」當成學習的關鍵環節，但使用者要的是**輸入端更好消化**，
不是輸出端被測驗。

### 量到什麼（`scratchpad/essence_audit.py`）

| 段落 | 覆蓋率 | 中位字數 | 在檔案裡的中位位置 |
|---|---|---|---|
| `## 精選摘要（沒時間先看這段）` | **161/163 (99%)** | 588 | **第 3,142 字元**（最深 6,625）|
| `## 融會貫通` | 161/163 | 1,887 | — |
| `## 乾貨摘要` | 126/163 | 562 | — |
| `## 導讀` | 55/163 | 1,759 | — |
| `## 邏輯拆解` | 70/163 | 1,744 | — |

**核心發現**：使用者要的「精粹」**早就存在**——那個標題字面上寫著「沒時間先看這段」、
99% 覆蓋、只有 588 字的段落。問題是它①在檔案裡被埋在三千多字之後
②**前端從來沒有暴露過**（`index.html` / `catchup.html` 提及次數 **0**，沒有任何 endpoint 吐它；
後端唯一用到它的地方是拿去當考自己的批改對照，`web_server.py:869`）。

### 決定（第二意見：已過 — Codex，收據 sha256=13c10caff9ec34f3，551 秒）

**我原本要「再產一層 250–400 字的精粹」→ 被否決，而且我自己在送審時就標出這是最大破綻**：
這個系統的病是**層太多**，用「再加一層」當藥是錯的。

Codex 的正解：**儲存層保持豐富，入口只呈現一條路**。用既有欄位組合，零 LLM：

- `thesis`（frontmatter，163/163）→ 一句主張
- `## 精選摘要 → ### 核心重點` 前 3 點（161/163）
- 2 篇沒有精選摘要的舊檔 → 退回 `## 乾貨摘要` 的時間戳 beats
- `weekly_action`（frontmatter，163/163）→ 一個可做的動作

它也否決了另外兩項：**補齊四層**（238 次 LLM 呼叫，只會讓非核心層更完整，
不減少任何一次選擇或一行閱讀量）、**刪除既有內容**（把內容藏起來就有同樣效果，
沒理由先破壞 163 篇資料）。

**它同時更正我三個誤判**：「四層只有 33% 齊全」「整篇中位 6,406 字」「檔案段落順序不一致」
——這些**都不是缺陷**，因為權威入口是瀏覽器不是檔案；只要 UI 自己決定預設顯示什麼，
檔案有多長不構成注意力成本。我另外量錯了段落長度（沒有像 endpoint 那樣在下一個 `##` 截斷）。

### 改了什麼

- `web_server.py`：新增 `extract_essence(body)`（精選摘要 → 乾貨 fallback → 無），
  結果放進 `collect_summaries()` 的 `essence_points` / `essence_source`，
  所以 `/` 與 `/api/summaries` **同一個資料來源**。
  順手把 `collect_summaries` 從「同一個檔讀三遍」（parse_frontmatter / extract_elevator_pitch
  各讀一次，再加精粹就是第三次 = 163 篇 489 次）改成讀一次傳下去（新增 `split_body`）。
- `templates/index.html`：
  - 卡片**收合**＝標題 ＋ 一句 `thesis`；**點一下原地展開**＝ 3 個精粹重點 ＋ 本週行動。
    不開 modal、不跳頁、不呼叫 LLM。
  - `weekly_action` 從卡片正面移進展開層（收合時畫面更乾淨）。
  - 卡片正面的 elevator pitch 拿掉（與 thesis 重複）。
  - 展開層底部加「還想深入？→ 展開工具列」，導讀／邏輯拆解／乾貨／考自己／心智圖
    全部收在那一個入口後面。
  - **首頁抽考 banner 撤除**；`randomChallenge()` / `renderRecallStats()` 連同 CSS 一併移除
    （唯一呼叫點沒了）。**自評與 localStorage 紀錄保留**，考自己回到「⋯」工具列。
  - 兩條渲染路徑（Jinja 與 `renderCards()`）同步改。

### 驗了什麼

| 項目 | 結果 |
|---|---|
| 精粹覆蓋 | **163/163**（161 精選摘要 + 2 乾貨 fallback），每篇都剛好 3 點 |
| 精粹字數 | 中位 **263** 字（168–378） |
| LLM 呼叫 | **0** |
| `outputs/summaries/*.md` | 165 檔 sha256 **與今天開工前完全一致** |
| 首頁大小 | 878 KB → 983 KB（精粹內嵌的代價；仍遠低於今天開工前的 1,294 KB） |

瀏覽器實測（**SSR 與轉檔後 `renderCards()` 兩條路徑數值完全相同**）：
`cards=163 essence=163 none=0 threePts=163 collapsedBodyHidden=true thesisVisible=true
expandInPlace=true modalOpened=false toolsHiddenBefore=true toolsShownAfter=true recallBanner=false`

靈敏度控制（不是只看綠燈）：
- 抽取函式三種輸入 → 有精選摘要／只有乾貨／兩者皆無，分別回 `精選摘要`／`乾貨摘要`／`(無)`
- 樣板 else 分支用假資料實測：**會印出「這篇還沒有精粹」，不會靜默空白**，也不會誤印空清單
- 圖示探針沿用先前那支（把 sprite id 改壞的對照組回 0.0×0.0），確認這次改動沒動壞圖示

### 還沒解決的

- **「263 字是否真的讓吸收更好」無法從點擊或停留時間誠實推導。** 這版是可逆的純 UI 改動，
  先照常用，不為了量吸收再塞問卷或測驗。
- `/catchup` 名為快讀，但預設展開全部 163 篇、同時顯示融會貫通與乾貨，HTML 約 1.48 MB
  ——與低注意力入口矛盾，列為下一個 P1。
- 內文仍不可搜（只能搜標題／講者／tag）。

---

## 2026-08-16（上午）— 冷啟動瘦身 + 抽考探針

### 起因

使用者回報：「隨著內容越來越多，冷啟動的時間拉長」，並提出解法方向
「先載入摘要，要顯示全部內容的話點進去再挖出來」。

### 量到什麼（先量再改，母體＝163 篇摘要）

| 成分 | 改前 | 隨篇數成長？ |
|---|---|---|
| `import web_server`（連帶 `import torch` / `whisper` / `openai`） | **2.7–6.6 秒** | ❌ 固定 |
| `collect_summaries()` 讀 165 個 .md + YAML | 0.17 秒 | ✅ 約 1 ms/篇 |
| `GET /` 產生 HTML | 0.20 秒 / **1,294 KB** | ✅ 約 8 KB/篇 |
| 瀏覽器建 DOM | 11,297 節點 | ✅ 約 2 ms/篇 |
| 檔案 I/O 是不是瓶頸（讀 163 個沒碰過的 transcripts, 7 MB） | 0.021 秒 = 0.1 ms/檔 | ❌ **已排除** |

**結論與使用者的假設不同**：冷啟動 ≈ 3.8–6.5 秒，其中約 **85% 是為了顯示一份靜態清單
而載入 torch**；真正隨篇數成長的只有約 0.5 秒。使用者提的「內容延後載入」方向，
其實系統**早就是這樣**（導讀／乾貨／邏輯拆解都是點了才 fetch），首屏被灌進來的
只有卡片外殼——所以真正要瘦的是外殼，不是內容。

**HTML 為什麼肥**：每張卡片內嵌 8–9 份完整 inline SVG 圖示，163 張＝1,398 份，
佔首頁 HTML 的 **44.7%**。而且同一份圖示markup **在兩個地方各寫了一次**：
Jinja 的伺服器渲染，以及轉檔後重建卡片的 `renderCards()`。

### 決定（第二意見：已過 — Codex 稽核，收據 sha256=9d614964e2734f27）

Codex 推翻我三處，我照它的結論做：

1. **我原本要加「背景執行緒預熱 torch」→ 取消。** 它做了成對實測：不預熱時
   `GET /` 0.197–0.214s，背景同時 import torch 時 **0.239–0.269s**（反而慢 25–70 ms）。
   預熱只是把成本搬到背景，沒有消除，還跟首屏搶資源。
2. **我原本只打算延後 `transcriber` / `summarizer` → 範圍不夠。** 它量到只搬這兩個是
   0.89 秒，把 openai / enrich_* / recategorize 也移出啟動路徑才是 0.27 秒。
3. **我原本排在第 4 的「卡片分批渲染 / 虛擬清單」→ 現在不做。** 163 篇只值約 0.3 秒，
   卻要把搜尋、分類計數、隱藏狀態、改名、刪除、轉檔後重建的風險全部翻新。
   先做 1＋3，之後若仍感覺到「篇數變多就變慢」再談。

學習面另外送審一次（收據見 `~/.claude/dispatch/logs/20260816-104655-197262_review.log`），
四個候選（SRS／跨篇概念索引／全文檢索／行動板）的結論：

- **先做「一題抽考探針」，不要一開始就做完整 SRS。** 理由：SRS 的價值全押在
  「使用者會不會每天回來」，而這件事**目前零證據**——現有的「考自己」打完就丟，
  沒有任何紀錄可查。先用最小成本量這個未知數。
- **跨篇概念索引（原本我排第 2）被實測否決**：163 篇抽出 786 個概念名，
  只有 **10 個（1.3%）**出現在 2 篇以上（對照組：tags 是 8.8%）。
  直接做反向索引會得到 786 個單篇節點，是名詞目錄不是知識圖，
  除非先做概念正規化。
- **行動板判定為假動作**，不做。
- 全文檢索（現在只能搜標題／講者／tag，內文搜不到）排下一輪。

### 改了什麼

**冷啟動**

- `transcriber.py`：`torch` / `whisper` 移進 `get_whisper_model()`、`yt_dlp` 移進
  `download_audio()`。**邊界畫在源頭**——`summarizer.py` 也 import 本模組，
  所以只改 web_server 沒有用。
- `summarizer.py` / `web_server.py` / `enrich_intro` / `enrich_digest` / `enrich_logic`
  / `enrich_synthesis` / `recategorize`：`from openai import OpenAI` 一律移進
  真正建立 client 的那個函式，並加 `from __future__ import annotations`
  ＋ `TYPE_CHECKING`（型別註解會在載入時求值，不處理會 NameError）。
  `gen_mindmap.py` / `enrich_summary.py` 不在 dashboard 啟動路徑，未動。
- `templates/index.html`：9 個卡片圖示改成頁首一份 `<defs>` sprite ＋ `<use>` 引用，
  **兩條渲染路徑（Jinja 與 `renderCards()`）都改**，否則轉完一篇重建後會退回舊樣。
- `開啟UI.bat`：原本 `start "" http://localhost:5000` 在啟 server **之前**，
  所以每次冷啟動瀏覽器必定先撞到 ERR_CONNECTION_REFUSED，要自己按重新整理。
  改成先啟 server，另起 `wait_and_open.py` 等 port 通了才開瀏覽器。
  同時把檔案改回純 ASCII（原本含 30 個非 ASCII 位元組，違反本機 .bat 規則）。
- `run-ui.sh`：同一個問題的 Mac/Linux 版（原本用 `sleep 2` 猜時間），改用同一支等待器。
- 新增 `wait_and_open.py`。

**學習面：抽考探針**

- `templates/index.html`：首頁加「🎯 抽考一篇（約 2 分鐘）」，隨機挑一篇未隱藏的
  演講、重用既有的「考自己」modal（**沒有新頁面、沒有新 API**）。
- 批改結果下方加自評三鈕（忘了／模糊／記得），記進 `localStorage.yt_recall_log`
  （`{filename, rating, completed_at}`）。首頁顯示「近 14 天：抽考 N 次 · 用了 M 天」。
- **刻意不做**：ease、streak、SM-2、每日佇列、server 端 schema。
  另一個理由是現有 `/challenge` 回的 `got_right[]` / `missed[]` 長度由 LLM 自由決定，
  **拿它換算成 SM-2 成績是假精確**；排程只能用使用者自評。

### 驗了什麼（都在真實入口，沒設額外環境變數）

| 項目 | 改前 | 改後 |
|---|---|---|
| 雙擊啟動 → port 5000 可連 | 3.25 秒 | **0.38 秒** |
| `GET /` HTML 大小 | 1,294 KB | **878 KB**（−32%） |
| 首頁 DOM 節點數 | 11,297 | **8,215**（−27%） |
| `GET /` 伺服器耗時 | 0.204 秒 | 0.204 秒（未變，本來就不是瓶頸） |

- **重構零 delta**：9 個圖示逐一量 `getBBox()`，改前改後**數值完全相同**
  （logic 18.0×18.0、hide 22.0×22.0、menu 17.4×3.4…）。
  ⚠️ 這個結論之所以可信，是因為先做了靈敏度校驗：把 sprite id 故意改錯的對照組
  量到 0.0×0.0。**第一版探針用 `getBoundingClientRect()`，改壞了也照樣回 20×20
  ——那種量測沒有辨識力，不能當通過。**
- **抽考探針行為**：抽考鈕開得起 modal 並選中某篇 ✓；記錄兩筆 → 「抽考 2 次 · 用了 2 天」✓；
  走 `rateRecall()` → 變 3 次且按鈕列換成「已記錄」✓；塞一筆 20 天前的**不算進 14 天窗** ✓；
  清掉 localStorage key → 統計歸零 ✓（靈敏度控制）。
- **轉檔路徑沒被改壞**：`download_audio()` 走到真正的下載失敗（代表 yt_dlp 延後 import 生效，
  不是 NameError）；`get_whisper_model('small')` 3.4 秒載入成功；
  `POST /challenge` 實打 DeepSeek 10.9 秒正常回覆。
- **163 篇摘要零改動**：`outputs/summaries/` 全部 165 個 .md 的 sha256 前後完全一致。
- 全部 `.py` 編譯通過；`一鍵啟動.bat` 鏈路 6 支腳本 import 全部 OK；
  `開啟UI.bat` 實際位元組：非 ASCII 0、CRLF 18、裸 LF 0。

### 下一輪的判準（不要憑感覺決定）

1. **抽考探針滿 14 天後看「不同使用日數」**（不是總次數）：≥ 4 天才升級成完整的
   間隔重複排程；否則就是這個功能不符合實際使用習慣，該砍不該補。
2. **全文檢索**是排最前面的下一個功能（現在內文搜不到）。實測讀完 163 篇只要 0.017 秒，
   暴力搜就夠，不需要建索引。
3. **跨篇概念索引**要先解決概念正規化（1.3% 重疊率），沒解決前不要做。
4. 若使用者在 P1＋P3 之後**仍**明確感覺「篇數變多就變慢」，才啟動卡片分批渲染；
   屆時注意現行 `applyFilter()` 是對 DOM 過濾的，沒建 DOM 的卡片會搜不到。
